# Content conventions

Keep long-form writing in `content/writing/` as MDX files with frontmatter.
Use `lib/content.ts` to read metadata when building listings or archives.

This folder is reserved for source content and should stay separate from UI.